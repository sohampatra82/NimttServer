<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link rel="shortcut icon"
        href="https://ik.imagekit.io/7de5cntkw/1692626704665-photoaidcom-cropped%20(1).jpeg?updatedAt=1738048156918"
        type="image/x-icon">
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background: linear-gradient(to right, #6a11cb, #2575fc);
            color: white;
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 10px;
        }

        p {
            font-size: 1.2rem;
            margin-bottom: 30px;
        }

        .container {
            display: flex;
            justify-content: center;
            gap: 10px;
        }

        .option {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 10px;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .option:hover {
            transform: scale(1.05);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
            border-radius: 4px;
        }

        .option button {
            background-color: transparent;
            /* Transparent background */
            color: #ffffff;
            /* White text */
            border: 2px solid #ffffff;
            /* White border */
            padding: 12px 0;
            font-size: 16px;
            font-weight: bold;
            border-radius: 10px;
            /* Softer corners */
            cursor: pointer;
            transition: background-color 0.3s, color 0.3s, border-color 0.3s;
            width: 150px;
            height: 50px;
            text-transform: uppercase;
            /* Uppercase text for emphasis */
        }

        .option button:hover {
            background-color: #ffffff;
            /* White background on hover */
            color: #2575fc;
            /* Blue text matching gradient */
            border-color: #2575fc;
            /* Blue border matching gradient */
        }
    </style>
</head>

<body>
    <br><br>
    <h1>Welcome to the Admin And Accountant Page</h1>
    <p>Please choose an option below</p>
    <div class="container">
        <form action="/accountantlogin" method="post">
            <div class="option">
                <button id="account" name="account"
                    onclick="window.location.href='/AccountantIndex'">Accountant</button>
            </div>
        </form>
        <form action="/adminlogin" method="post">
            <div class="option">
                <button id="admin" name="admin" onclick="window.location.href='/AdminIndex'">Admin</button>
            </div>
        </form>
    </div>
</body>

</html>